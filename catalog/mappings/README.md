# Mappings

Mappings between frameworks.

- `iso27002_to_nist_csf.yml` : ISO 27002 controls → NIST CSF categories/subcategories (progressive build)
- `cis_to_iso_nist.yml` : CIS controls/safeguards → ISO/NIST

NB: mappings are approximate; always validate against official texts.
